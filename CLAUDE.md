# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code 向けのプロジェクト指針です。
グローバルルール（`~/.claude/rules/*`）に加えて、本プロジェクト固有の方針をここで定義します。

## プロジェクト概要

旅費精算用の **経路表示 CLI**。出発駅・到着駅・時刻（到着 or 出発）を指定すると、
Yahoo!乗換案内 から経路を取得し、次の形式の **1行テキスト** を出力する。

```text
分倍河原(13:19)→新宿(13:36)→日暮里(14:05)→成田空港第一ターミナル(14:43)
```

さらに、ユーザーが選択したルートの **Yahoo!乗換案内ページを PDF として保存** する
（経費精算の証憑用）。

### 想定ユースケース（ユーザーの実態）

- 出発駅は **分倍河原** または **溝の口** が定番（プリセット化する）
- 行き先は **成田空港** または **羽田空港** が多い（プリセット化する）
- **到着時刻指定**（例: 「成田に X 時までに着きたい」→ 逆算）と
  **出発時刻指定**（例: 「分倍河原を X 時に出る」）を **両方同等に** 使う

## データ源

**Yahoo!乗換案内（`https://transit.yahoo.co.jp`）** の検索結果ページを取得・解析する。

- Yahoo は無料の公式「経路検索 API」を提供していないため、**Web ページのスクレイピング** で実装する。
- 検索結果ページ URL（要パラメータ検証）:
  `https://transit.yahoo.co.jp/search/result?from=...&to=...&y=...&m=...&d=...&hh=...&m1=...&m2=...&type=...`
  - `type`: 出発/到着の指定種別（**実リクエストを確認して確定する**。一般に `1`=出発時刻指定 / `4`=到着時刻指定 と言われるが要検証）
  - 時刻は `hh`（時）と `m1`/`m2`（分の十の位・一の位）に分割される形式 → 実物で確認
- 取得した HTML から候補ルート（区間・発着時刻・所要時間・運賃・乗換回数）を構造化する。

### 実機調査で確認済みの仕様（2026-06 時点 / `tests/fixtures/` に保存）

検索結果ページ: `GET https://transit.yahoo.co.jp/search/result`（HTTP 200 で HTML を返す）

| パラメータ | 意味 | 例 |
| --- | --- | --- |
| `from` / `to` | 出発駅 / 到着駅（駅名・URLエンコード） | 分倍河原 / 成田空港 |
| `y` / `m` / `d` | 年 / 月(2桁) / 日 | 2026 / 06 / 20 |
| `hh` | 時 | 13 |
| `m1` / `m2` | 分の十の位 / 一の位（例: 19分→ `m1=1,m2=9`） | 1 / 9 |
| `type` | **`1`=出発時刻指定 / `4`=到着時刻指定**（確認済み） | 1 |
| `ex` | **`1`=有料特急（スカイライナー/NEX 等）を候補に含める**（確認済み） | 1 |

> `via01`（経由駅）はフォームに存在するが、駅名を渡しても経由強制は効かない（無視される）。
> 経由条件はルート選択側のフィルタで実現する。

結果 HTML の構造（候補ルートごとに繰り返し）:

- ルート 1 件 = `div.routeDetail` ブロック（ページ内に複数並ぶ）。
- 駅: `dt.station` のテキスト
  - 先頭駅: `HH:MM 発 駅名 …`
  - 中間駅: `HH:MM 着 HH:MM 発 駅名 …`
  - 末尾駅: `HH:MM 着 駅名 …`
- 路線: `dt.access` のテキスト（`京王線 新宿行` のように 路線名＋方向）。
- 徒歩区間: `.transport` = `徒歩`。
- サマリ: `li.time`（`HH:MM 発→ HH:MM 着 2時間11分（乗車 1時間44分）`）。
  `div.routeSummary` に 早/安/楽 ラベル・運賃・乗換回数。
- 検索条件の確認: ページ冒頭に `2026年06月20日(土) 14:43 到着` のような条件表示あり。

> 注意: 駅名は表記揺れ・カッコ補足あり（例: `本八幡(都営線)` `成田空港(東京)` `空港第２ビル(鉄道)`）。
> 出力整形時に扱う。`始発`/`終電` 等の `type` 値や、ルート個別 permalink は未確認（必要時に調査）。

### 順守事項（重要）

- **個人利用の範囲** に限定する。大量・高頻度アクセスはしない。
- リクエストには適切な **間隔（スリープ）** を入れ、**User-Agent** を明示する。
- 取得結果は可能な範囲で **キャッシュ** し、同一クエリの再取得を避ける。
- robots.txt / 利用規約を尊重する。HTML 構造変更で壊れる前提で、**パーサは疎結合に** 設計する。

## PDF 保存

選択ルートの Yahoo ページを **そのまま PDF 化** する（自前レイアウトではなく実ページを証憑化）。

- **Playwright（ヘッドレス Chromium）** でページを開き、`page.pdf()` で印刷する。
- 保存ファイル名は内容から決定（例: `2026-06-17_分倍河原-成田空港_1443着.pdf`）。
- ルート選択は Yahoo 結果ページ内の特定候補に対応する URL/状態を開いてから PDF 化する。

## 技術スタック（デフォルト決定 / 要確認は明記）

- **言語**: Python 3.13
- **パッケージ/環境管理**: `uv`（`pyproject.toml`）
- **HTTP 取得**: `httpx`
- **HTML 解析**: `beautifulsoup4` + `lxml`
- **PDF 生成**: `playwright`（Chromium）
- **CLI フレームワーク**: `typer` + 表示に `rich`
- **設定ファイル**: TOML（標準 `tomllib` で読み込み）。駅プリセットを定義
- **テスト**: `pytest`（HTML は固定フィクスチャでパーサをテストし、ネットワークに依存させない）

> 上記のうち「要確認」項目は実装前にユーザー確認を取ること。それ以外は本ファイルの決定に従う。

## アーキテクチャ / ファイル構成

機能ドメインで分割し、小さく保つ（1ファイル 200–400 行目安、最大 800）。

```text
tofrom-time/
├─ CLAUDE.md
├─ pyproject.toml
├─ README.md
├─ config/
│  └─ stations.toml          # 駅プリセット（分倍河原/溝の口/成田/羽田 等）
├─ src/tofrom_time/
│  ├─ __init__.py
│  ├─ cli.py                 # typer エントリポイント、引数解釈
│  ├─ config.py              # stations.toml 読み込み・検証
│  ├─ query.py               # 検索条件（出発/到着・時刻・駅）を表す不変データ
│  ├─ yahoo/
│  │  ├─ url.py              # 検索 URL 組み立て
│  │  ├─ client.py           # httpx 取得（UA・スリープ・キャッシュ）
│  │  └─ parser.py           # HTML → Route 構造化
│  ├─ models.py              # Route / Segment 等の不変データクラス
│  ├─ render.py              # 1行テキスト整形
│  └─ pdf.py                 # Playwright で選択ルートを PDF 化
└─ tests/
   ├─ fixtures/              # 取得済み HTML サンプル
   └─ test_*.py
```

## CLI 設計（暫定 / 実装時に確定）

```bash
# 出発時刻指定
tofrom-time --from 分倍河原 --to 成田空港 --depart 13:19
# 到着時刻指定
tofrom-time --from 溝の口 --to 羽田空港 --arrive 14:43
# 日付指定（省略時は当日）
tofrom-time --from 分倍河原 --to 成田空港 --arrive 14:43 --date 2026-06-20
# PDF も保存
tofrom-time --from 分倍河原 --to 成田空港 --arrive 14:43 --pdf ./out/
```

- `--depart` と `--arrive` は排他。両方/どちらも無い場合はエラーで明示。
- 駅名はプリセット別名（例: `narita` → 成田空港）も受け付ける。
- 複数候補が返る場合は一覧表示し、番号で選択 → 選択ルートをテキスト出力＆（指定時）PDF 化。

## 出力フォーマット

- **既定**: 1行テキスト（`駅(発時刻)→駅(着時刻)→…`）。
- 駅名は乗換が発生する駅と始発・終着を表示（直通区間の途中駅は省略）。
- `--json` オプションで構造化出力も可能にする（精算ツール連携用）。

## ルート選択条件（preferences）

行き先ごとに「使いたい列車・経由」を `config/stations.toml` の `[preferences.*]` で定義し、
取得した候補を絞り込む。キーが行き先の正式駅名と一致すると自動適用される。

- `any_lines`: いずれかの区間の路線名にこの語のどれかを含むルートに絞る
- `via`: これらの駅をすべて通るルートに絞る（駅名の部分一致）
- `use_limited_express`: 検索時に有料特急を含める（`ex=1`）

既定の定義:

- **成田空港**: `use_limited_express=true` ＋ `any_lines=[スカイライナー, 成田エクスプレス]`
- **羽田空港**: `any_lines=[京急]` ＋ `via=[渋谷]`

挙動メモ: 条件に合うルートが 0 件なら警告して全候補にフォールバックする（取りこぼし防止）。
`via=[渋谷]` は溝の口発では自然に一致するが、分倍河原発では京急が新宿〜品川経由になり
一致しないことがある（その場合フォールバック）。`--prefer NAME` で明示指定、`--all` で無効化。

## コーディング規約（本プロジェクト適用）

グローバルの `~/.claude/rules/coding-style.md` に従う。特に本プロジェクトで重視する点:

- **不変性**: `Route`/`Segment`/`Query` は `@dataclass(frozen=True)`。状態を破壊的に変更しない。
- **境界での検証**: 駅名・時刻・日付・外部 HTML は信頼しない。パース失敗は握りつぶさず明示エラー。
- **疎結合パーサ**: Yahoo の HTML 変更に備え、取得（client）/ URL 構築 / 解析（parser）を分離。
- **ネットワーク非依存テスト**: パーサは `tests/fixtures/` の保存済み HTML に対してテストする。

## 開発ワークフロー

- TDD を基本とする（`~/.claude/rules/testing.md`）。まずパーサのテストをフィクスチャで固める。
- HTML 構造調査時は、実ページを 1 度取得して `tests/fixtures/` に保存し、以後それで開発する。
- 破壊的・外向き操作（実サイトへの連続アクセス等）は事前確認。

## 確定済みの方針

- 環境管理は **`uv`**。
- PDF 化の対象は **選択した 1 ルートの詳細ページ**。
- Yahoo へのアクセスは **個人利用・低頻度** に限定。

## 未確定事項

1. `始発`/`終電` 等の `type` 値（必要になれば調査）
2. キャッシュの TTL（現状は URL ハッシュkeyで無期限。時刻表は日付指定なら安定だが要検討）

### 解決済み

- ルート選択 → PDF 対象ページ: 各ルートの「印刷する」リンク
  `/search/print?...&no=N`（`no` がルート番号）。これを Playwright で開いて PDF 化する。
